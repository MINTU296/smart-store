# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
build/
dist/
develop-eggs/
downloads/
eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt

# ---------- Virtual environments ----------
.venv/
venv/
env/
ENV/
.python-version

# ---------- Testing & coverage ----------
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.cache/
coverage.xml
*.cover
*.py,cover
.hypothesis/

# ---------- Environment / secrets ----------
.env
.env.*
!.env.example

# ---------- Node / React / Vite ----------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.yarn/
.parcel-cache/

# Vite build output for the dashboard. The committed bundle in
# dashboard/assets/ is the production artifact that ships with the API
# image (the API serves it as static files; there is no Node toolchain
# in the container). Rebuild it locally with
# `cd dashboard-src && npm run build` and commit the new hashed filenames.
dashboard-src/dist/

# TypeScript build artifacts
*.tsbuildinfo
dashboard-src/tsconfig.tsbuildinfo

# ---------- IDE / editors ----------
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/
.classpath

# ---------- OS junk ----------
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ---------- Docker / runtime ----------
*.pid
*.log
logs/

# ---------- Data & generated artifacts ----------
data/Store*/
data/*.parquet
data/*.sqlite
data/*.db
*.mp4
*.avi
*.mov
*.mkv

# ---------- Claude / local tooling ----------
.claude/
